<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :artist, class: "control" %>
    <%= form.text_field :artist, class: "control input" %>
  </div>

  <div class="field">
    <%= form.label :album %>
    <%= form.text_field :album, class: "control input"  %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, class: "control input"  %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, class: "control input"  %>
  </div>

  <div class="actions">
    <%= form.submit 'Create Product', class: "button is-info" %>
  </div>
<% end %>
