
  <h1 class="title">
    <%= @article.title %>
  </h1>

  <p class="subtitle">
    <%= @article.description %>
  </p>

<% if signed_in? && current_user.admin? %>
  <%= link_to 'Edit', edit_article_path(@article) %> |
<% end %>
<%= link_to 'Back', articles_path %>
